{
  "name": "howto-tfjs",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "lint": "lerna run --parallel --no-bail lint"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "tf-*",
    "packages/*"
  ]
}
